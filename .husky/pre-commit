#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
pnpm lint-staged

# Run tests
echo "🧪 Running tests..."
pnpm test --passWithNoTests --bail

echo "✅ Pre-commit checks passed!"
